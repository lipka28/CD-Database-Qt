##CDDatabase

Basic Qt based CD(or whatever) database.

#Build instructions

Requirements:

	- g++ (at least 5.4)
	- gcc (at least 5.4)
	- Qt5 (5.10.1 tested)
	- qmake

Compilation (Linux):

	1. invoke qmake in the directory
	2. invoke make
